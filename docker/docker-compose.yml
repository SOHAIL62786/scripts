version: "3.9"

services:

  app:
    build: .
    container_name: nodeapp
    ports:
      - "3000:3000"
    depends_on:
      - mongodb

  mongodb:
    image: mongo:6
    container_name: mongodb
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
